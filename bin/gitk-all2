#!/usr/bin/perl

my @args;

open(my $fh, "-|", "git", "branch") || die;
while(<$fh>)
{
    chomp;
    s/^..//;
    next if($_ eq "(no branch)");

    push @args, $_;
}
close($fh) || die;

exec("gitk", @args, @ARGV);
